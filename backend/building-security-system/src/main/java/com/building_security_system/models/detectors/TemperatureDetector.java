package com.building_security_system.models.detectors;

public class TemperatureDetector extends Detector {
    public TemperatureDetector(long id) {
        super(id, TemperatureDetector.class.getName());
    }
}